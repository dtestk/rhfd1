apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: simplenodeservice-rhfd1-prod-destination
spec:
  host: simplenodeservice.rhfd1-prod.svc.cluster.local
  subsets:
  - name: blue
    labels:
      deployment: simplenodeservice-blue
  - name: green
    labels:
      deployment: simplenodeservice-green